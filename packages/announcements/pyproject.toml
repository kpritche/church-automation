[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "church-automation-announcements"
version = "0.1.0"
description = "Generate ProPresenter announcement slides from website"
readme = "README.md"
requires-python = ">=3.9"
authors = [
    {name = "Church Automation", email = "contact@example.com"}
]
dependencies = [
    "church-automation-shared",
    "google-genai>=0.1.0",
    "beautifulsoup4>=4.12.0",
    "qrcode[pil]>=7.4.0",
    "pillow>=10.0.0",
    "requests>=2.31.0",
    "protobuf>=4.0.0",
    "python-pptx>=0.6.21",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0.0",
    "black>=23.0.0",
    "mypy>=1.0.0",
]

[project.scripts]
make-announcements = "announcements_app.main:main"

[tool.setuptools.packages.find]
where = ["."]
include = ["announcements_app*"]
